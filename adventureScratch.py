#import envMaker
#import comandos
import numpy as np

arquivo = np.genfromtxt("cenas.txt", dtype=None, delimiter=";")

#precisamos de uma função que sempre escuta o que o jogador tem a dizer
    def file_find(cenas):
    return arquivo[cenas]

while True:
    action = input("> ")
    cena
    x = file_find(cena))
    print(x)
